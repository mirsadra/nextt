// Run `rafce` to create a new component
// in Next.js, we no longer need to specify the react import
// import React from 'react'

const Home = () => {
  return (
    <div>Home</div>
  )
}

export default Home